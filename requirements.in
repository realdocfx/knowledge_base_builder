# Core dependencies
internetarchive[speedups]==5.10.1
typer==0.26.8
rich==14.1.0
requests==2.32.4
libzim==3.11.0

# Web dependencies
fastapi==0.139.2
uvicorn[standard]==0.51.0
httpx==0.28.1
python-multipart==0.0.20  # FastAPI form parsing for the lock-screen POSTs
# Note: xapian-bindings is provisioned via custom wheel, not from PyPI

# Content rendering + sandbox provisioning (audit P4: these are declared in
# pyproject -- pypdf core, pymupdf [web], pycdlib [sandbox] -- but were missing
# from the hash-pinned closure, so a stick provisioned by `kb-builder portable`
# shipped without server-side PDF rendering or the non-ZIM media ISO).
pypdf==6.14.2
pymupdf==1.28.0
pycdlib==1.16.0

# Post-Quantum Cryptography (FIPS 203/204/205)
dilithium-py==1.4.0
cryptography==45.0.4
argon2-cffi==25.1.0
